import { FC } from "react";

import { SectionWrapper } from "@components/SectionWrapper";

export const ExplanationsSection: FC = () => {
  return (
    <SectionWrapper title="Как это работает" margin="24px 0px 0px 0px">
      Как это работает
    </SectionWrapper>
  );
};
